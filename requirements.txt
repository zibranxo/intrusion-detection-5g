# 5G MEC IDS — on-device inference pipeline
# Install: pip install -r requirements.txt

# Core inference
ultralytics>=8.0.0          # YOLOv8 pose model + ONNX export
torch>=2.1.0                # FP16 autocast, CUDA/MPS backends
torchvision>=0.16.0

# ONNX quantization
onnx>=1.15.0
onnxruntime>=1.17.0         # CPU execution provider
onnxruntime-gpu>=1.17.0     # CUDA execution provider (GPU machines)
onnxruntime-tools>=1.7.0  # Uncomment for INT8 static quantization

# Vision & tracking
opencv-python>=4.9.0
deep-sort-realtime>=1.3.2   # DeepSORT persistent re-ID

# Numerics
numpy>=1.24.0

# Testing
pytest>=7.4.0
